<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Home</title>
</head>

<body>

    <h1>Hello now with 2d structure{{name}} {{lastName}}!</h1>
{{#with address}}
  <p>{{street}}, {{number}}</p>
{{/with}}

    <h1>Hello {{name}}!</h1>
    {{#if address}}
    <h2>This is displayed!!</h2>
    {{else}}
    <h2>This will be displayed because the
        "address" property does not exists!!</h2>
    {{/if}}
    <ul>
        {{#each cities}}
        <li>{{this}}</li>
        {{else}}
        <p>No cities yet!</p>
        {{/each}}
    </ul>


</body>

</html>